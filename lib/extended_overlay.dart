import 'package:flutter/material.dart';

class ExtendedOverlay extends StatelessWidget {
  final String path;
  const ExtendedOverlay({super.key, required this.path});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
